<!DOCTYPE html>
<html>
<head>
  <title>FreshBuilt</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Playfair+Display+SC' rel='stylesheet' type='text/css'>
</head>
<body>

  	<header style='background:yellow;'>
      <ul class='nav-bar'>
        <li class="active"><%= link_to 'home', root_path %></li>
        <li><%= link_to 'About Us', about_path %></li>
        <li><%= link_to 'Login', login_path if session[:user_id].nil? %></li>
        <li><%= link_to "Logout of #{@authenticated_user.email}", logout_path if session[:user_id] %></li>
        <li><%= link_to "My Profile", user_path(@authenticated_user) if session[:user_id] %></li>
        <li><%= link_to "Messages" if session[:user_id] %></li>
        <li><%= link_to 'Create User', new_user_path if session[:user_id].nil? %></li>
      </ul>
  	</header>

<div class='row'>
  <div class='three columns h' style='background: white;'>
    <%= render 'shared/subnav' if session[:user_id] %>
    <%= yield :match %>
  </div>
  <div class='nine columns h' style='background: #aaa;'>
  	<%= yield %>
  </div>
</div><!-- row -->

<footer style='background:green;'>
</footer>

</body>
</html>
