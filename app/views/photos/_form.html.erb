<%= form_for user_photos_path(@authenticated_user), :html => {:multipart => true} do %>

  <div class="field">
    <%= file_field_tag :photo %>
    <%= hidden_field_tag :photo_cache %>
    
  </div>

  <div class="submit"><%= submit_tag "Submit" %></div>

<% end %>